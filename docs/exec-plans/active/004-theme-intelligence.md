# Execution Plan: Theme Intelligence

Status: complete

Owner: agent

Last updated: 2026-07-01

## Goal

Productionize the current `theme-analysis.py` feature set: GPT theme generation, community transition comparison, Sankey diagrams, membership-change diagrams, and theme similarity heatmaps.

## Context

Read first:

- `theme-analysis.py`
- `docs/design-docs/theme-intelligence-contract.md`
- `docs/design-docs/pipeline-contract.md`
- `docs/verification/test-matrix.md`

This plan starts after matched LDA outputs are generated by Plan 003.

## Non-Goals

- Do not replace LDA with GPT.
- Do not require OpenAI calls in tests.
- Do not add a full dashboard.

## Files Expected To Change

- `src/themes/data_loader.py`
- `src/themes/gpt_theme_generator.py`
- `src/themes/community_transition.py`
- `src/themes/sankey_paths.py`
- `src/themes/membership_changes.py`
- `src/themes/theme_similarity.py`
- `src/visualization/community_transition.py`
- `src/visualization/theme_similarity.py`
- `src/pipelines/theme_pipeline.py`
- related tests

## Milestone 1: Theme Data Loading

Tasks:

- [x] Port `load_prepare_data`.
- [x] Preserve month sorting.
- [x] Preserve keyword parsing and de-duplication.
- [x] Preserve `all_keywords`, `absolute_keywords`, and `weighted_keywords`.

Validation:

```bash
pytest tests/unit/test_theme_data_loader.py
```

## Milestone 2: GPT Theme Generation

Tasks:

- [x] Wrap GPT calls behind provider interface.
- [x] Preserve model defaults.
- [x] Add mock provider for tests.
- [x] Preserve general, absolute, and weighted theme outputs.
- [x] Add optional response caching.

Validation:

```bash
pytest tests/unit/test_theme_generation_contract.py
```

## Milestone 3: Community Transition

Tasks:

- [x] Port Jaccard transition matching.
- [x] Preserve reply threshold `0.0`.
- [x] Preserve non-reply threshold `0.5`.
- [x] Preserve transition output columns.

Validation:

```bash
pytest tests/unit/test_community_transition.py
```

## Milestone 4: Sankey And Path Analysis

Tasks:

- [x] Port `get_path_info`.
- [x] Port directed graph building.
- [x] Port DFS path detection.
- [x] Port Sankey HTML/PNG visualization.

Validation:

```bash
pytest tests/unit/test_sankey_paths.py
make run-theme-sample
```

## Milestone 5: Membership Changes

Tasks:

- [x] Port `calculate_membership_changes`.
- [x] Port membership-change diagram generation.
- [x] Preserve existing/new/reappearing member semantics.

Validation:

```bash
pytest tests/unit/test_membership_changes.py
```

## Milestone 6: Theme Similarity

Tasks:

- [x] Port SentenceTransformer similarity calculation.
- [x] Make model configurable.
- [x] Port absolute, weighted, and general theme extraction.
- [x] Port heatmap visualization.

Validation:

```bash
pytest tests/unit/test_theme_similarity.py
```

## Acceptance Criteria

- [x] Theme pipeline can run from matched LDA monthly CSVs.
- [x] GPT calls are mockable and optional in tests.
- [x] Community transitions are saved.
- [x] Sankey HTML output is saved.
- [x] Membership-change calculations are tested.
- [x] Theme similarity heatmaps are generated or skipped with documented dependency issue.

## Progress Log

| Date | Update |
|---|---|
| 2026-06-23 | Created plan from inspected thesis code. |
| 2026-07-01 | Validation during Plan 003 surfaced two Plan 004 blockers: full unit collection fails because `tests/unit/test_gpt_themes.py` imports old `call_gpt_theme_api` / `generate_gpt_theme` symbols that are absent from `src/themes/gpt_themes.py`; a broader run excluding that file fails `tests/unit/test_heatmaps.py::test_calculate_sentence_similarity` because SentenceTransformer tries to reach Hugging Face offline. These remain out of scope for Plan 003. |
| 2026-07-01 | Completed offline-safe Plan 004 foundation: restored GPT compatibility wrappers without constructing clients, added `src/themes/data_loader.py`, made heatmap sentence similarity configurable/injectable with a deterministic offline fallback, aligned heatmap default model with `paraphrase-MiniLM-L6-v2`, and added data-loader tests. Validation: targeted theme foundation tests passed 9 tests; full `tests/unit` passed 58 tests offline. |
| 2026-07-01 | Completed the remaining offline Plan 004 slice: reconciled `src/themes/transitions.py` as a backward-compatible facade over canonical transition and Sankey modules, added in-memory LLM response caching, made `run-theme-analysis` config-driven, added offline tests for Sankey paths, membership changes, mock-provider theme pipeline execution, and theme similarity visualization, and made SentenceTransformer loading local-files-only before deterministic fallback. Validation: full `tests/unit` passed 64 tests; sample config validation passed; syntax compile passed. |
