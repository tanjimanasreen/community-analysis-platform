version: '3.8'

services:
  memgraph:
    image: memgraph/memgraph-platform:latest
    container_name: memgraph_thesis
    ports:
      - "7687:7687"
      - "3000:3000"
    volumes:
      - memgraph_data:/var/lib/memgraph

volumes:
  memgraph_data:
