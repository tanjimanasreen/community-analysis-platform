Based on the supplied ordered LDA keywords, identify one or more coherent themes represented by the evidence.

For each theme, return a concise, faithful, and descriptive theme name together with the explicit zero-based keyword IDs supporting that theme. Use only the bracketed IDs shown beside the supplied keywords; do not infer, renumber, or convert them to one-based positions.

Return only the requested structured JSON. Do not reproduce the supplied keyword strings in the response.

Here are the ordered keywords with their canonical zero-based IDs:
{{ keywords }}
